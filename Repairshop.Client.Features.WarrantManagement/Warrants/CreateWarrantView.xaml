<nav:ViewBase x:Class="Repairshop.Client.Features.WarrantManagement.Warrants.CreateWarrantView"
              x:TypeArguments="local:CreateWarrantViewModel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Repairshop.Client.Features.WarrantManagement.Warrants"
             xmlns:nav="clr-namespace:Repairshop.Client.Common.Navigation;assembly=Repairshop.Client.Common"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=local:CreateWarrantViewModel}">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <local:EditWarrantControl Grid.Row="0"
                                  ViewModel="{Binding EditWarrantViewModel}" />

        <Button Grid.Row="1"
                Content="Pokreni novi nalog"
                Command="{Binding CreateWarrantCommand}"/>

    </Grid>
</nav:ViewBase>
