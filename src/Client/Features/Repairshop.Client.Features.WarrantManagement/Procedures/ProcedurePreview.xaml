<UserControl x:Class="Repairshop.Client.Features.WarrantManagement.Procedures.ProcedurePreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="procedurePreview">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <md:Card>
            <Label Content="{Binding Procedure.Name, ElementName=procedurePreview}"
                   Background="{Binding Procedure.BackgroundColorBrush, ElementName=procedurePreview}"
                   Foreground="{Binding Procedure.ForegroundColorBrush, ElementName=procedurePreview}"
                   Padding="10" />
        </md:Card>

    </Grid>
</UserControl>
